apiVersion: apps/v1
kind: Deployment
metadata:
  name: education-provider-registry-data-maintenance
  labels:
    app: education-provider-registry-data-maintenance
spec:
  replicas: 2
  selector:
    matchLabels:
      app: education-provider-registry-data-maintenance
  template:
    metadata:
      labels:
        app: education-provider-registry-data-maintenance
    spec:
      nodeSelector:
        teacherservices.cloud/node_pool: applications
        kubernetes.io/os: linux
      containers:
      - name: education-provider-registry-data-maintenance
        image: ghcr.io/dfe-digital/education-provider-registry-data-maintenance:#MAINTENANCE_IMAGE_TAG#
        securityContext:
          allowPrivilegeEscalation: false
          runAsNonRoot: true
        ports:
        - containerPort: 8080
        resources:
          requests:
            memory: 64M
            cpu: 0.1
          limits:
            memory: 64M
            cpu: 1
