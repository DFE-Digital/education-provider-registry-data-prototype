apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: education-provider-registry-data-production.teacherservices.cloud
spec:
  ingressClassName: nginx
  rules:
  - host: education-provider-registry-data-production.teacherservices.cloud
    http:
      paths:
      - pathType: ImplementationSpecific
        backend:
          service:
            name: education-provider-registry-data-maintenance
            port:
              number: 80
